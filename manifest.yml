applications:
- name: dingo-watch-cluster-changes
  memory: 64M
  buildpack: go_buildpack
  stack: cflinuxfs2
  no-route: true
  health-check-type: process
  env:
    GOPACKAGENAME: dingo-watch-cluster-changes
